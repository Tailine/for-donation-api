version: '3'

services:
  db:
    image: postgres:14-alpine
    restart: always
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: for-donation
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret

volumes:
  postgres-data: